{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
<section class="row p-2 m-0 container-fluid" >
 <div class=" col-2  mobilefirst">
                <div class="container" id="displayOfNewProducts">
                  <div class="row">                  

                               {% for prod in productBrands %}
                                    {{ dump(prod.products) }}
                               {% endfor %}             
                    
{#                
                       <div class="d-flex justify-content-center col-xs-12 col-md-4">
                        <div class="text card mb-4 shadow p-3 mb-5  rounded">
                          <a href="{{ path('product_details', { brand.products}) }}"><img class="bd-placeholder-img card-img-top" width="100%" height="" src="{{ asset('uploads/' ~  newProduct.pictures[0].name) }}" preserveaspectratio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: Thumbnail"></a>
                          <title>{{brand.products.name}}</title>
                          <rect width="100%" height="100%" fill="#55595c"></rect>
                          <text x="50%" y="50%" fill="#eceeef" dy=".3em">{{brand.name}}</text>
                          {% if brand.statusPromotion %}
                          <text x="50%" y="50%" fill="#eceeef" dy=".3em" class="text text-warning">Promo</text>
                          {% endif %}
                          <text x="50%" y="50%" fill="#eceeef" dy=".3em"> Prix {{brand.products.inclTaxesPrice}}€</text>
                          <div class="card-body d-flex flex-column justify-content-between">
                            <p class="card-text d-flex align-items-start">{{newProduct.description}}</p>
                            <div class="d-flex justify-content-between">
                              <div class="btn-group">
                                <a href="{{ path('product_details', {id: brand.products.id}) }}" type="button" class="btn btn-sm btn-outline-secondary">Voir</a>
                                <a href=" {{path('cart_add', {id: brand.products.id})}}" type="button" class="btn btn-sm btn-outline-secondary">Panier</a>
                              </div>                            
                              <small class="text-muted">{{brand.products.name }}</small>
                              
                            </div>
                          </div>
                        </div>
                      </div>
                    {% endfor %} #}
                  </div>
            </div>
	</div>
  </section>

{% endblock %}
